import React, {Component} from "react";
import Button from "react-bootstrap/Button";

export default class HomePage extends Component {
    render () {
        return (
            <div>
                <h1>You are on the HomePage component</h1>
            </div>
        );
    }
}